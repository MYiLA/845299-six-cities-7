// import { AnyAction } from 'redux';
// import browserHistory from '../../browser-history';
// import { ActionTypeName } from '../action';

// export const redirect = () => (next: CallableFunction) => (action: AnyAction): CallableFunction => {
//   if (action.type === ActionTypeName.REDIRECT_TO_ROUTE) {
//     browserHistory.push(action.payload);
//   }

//   return next(action);
// };
